services:
  - type: web
    name: mechmind-backend
    runtime: node
    region: frankfurt
    branch: main
    rootDir: backend
    buildCommand: npm install && npx prisma generate && npx tsc
    startCommand: node dist/main
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:fudpe6-jastuf-nupzeT@db.sezvlnxoafyjxidlpwuy.pooler.supabase.com:5432/postgres?sslmode=require
      - key: JWT_SECRET
        value: mechmind-super-secret-key-2026-production
      - key: JWT_REFRESH_SECRET
        value: mechmind-refresh-secret-key-2026-production
      - key: FRONTEND_URL
        value: https://mechmind-os.vercel.app
      - key: CORS_ORIGIN
        value: https://mechmind-os.vercel.app
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
