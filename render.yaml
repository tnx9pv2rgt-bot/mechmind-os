services:
  - type: web
    name: mechmind-backend
    runtime: node
    region: frankfurt
    branch: main
    rootDir: backend
    buildCommand: npm install --legacy-peer-deps && npx prisma generate && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:fudpe6-jastuf-nupzeT@db.sezvlnxoafyjxidlpwuy.pooler.supabase.com:5432/postgres?sslmode=require
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://mechmind-os.vercel.app
      - key: CORS_ORIGIN
        value: https://mechmind-os.vercel.app
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
