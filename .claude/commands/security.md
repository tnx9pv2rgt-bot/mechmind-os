Security audit completo. Usa la skill `/security-review`, esegui tutti i comandi grep, e stampa:

```
SECURITY AUDIT — MechMind OS v10
Query senza tenantId:  X (Y child models OK, Z da fixare)
Webhook senza firma:   X
PII non cifrati:       X
Secrets hardcoded:     X
```

Per ogni issue → fixa immediatamente.

$ARGUMENTS
