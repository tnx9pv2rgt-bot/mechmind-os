import {
  Controller,
  Get,
  Post,
  Patch,
  Delete,
  Body,
  Param,
  Query,
  UseGuards,
  HttpCode,
  HttpStatus,
  ParseUUIDPipe,
} from '@nestjs/common';
import { ApiTags, ApiBearerAuth, ApiOperation } from '@nestjs/swagger';
import { JwtAuthGuard } from '../../auth/guards/jwt-auth.guard';
import { TenantId } from '../../common/decorators/tenant-id.decorator';
import { __MODULE_NAME__Service } from '../services/__MODULE_KEBAB__.service';
import { Create__MODULE_NAME__Dto } from '../dto/create-__MODULE_KEBAB__.dto';
import { Update__MODULE_NAME__Dto } from '../dto/update-__MODULE_KEBAB__.dto';

@ApiTags('__MODULE_PLURAL__')
@ApiBearerAuth('JWT-auth')
@UseGuards(JwtAuthGuard)
@Controller('v1/__MODULE_PLURAL__')
export class __MODULE_NAME__Controller {
  constructor(private readonly service: __MODULE_NAME__Service) {}

  @Get()
  @ApiOperation({ summary: 'Lista __MODULE_SINGULAR_IT__' })
  async findAll(
    @TenantId() tenantId: string,
    @Query('page') page?: number,
    @Query('limit') limit?: number,
  ) {
    return this.service.findAll(tenantId, { page: page ?? 1, limit: limit ?? 20 });
  }

  @Get(':id')
  @ApiOperation({ summary: 'Dettaglio __MODULE_SINGULAR_IT__' })
  async findOne(
    @TenantId() tenantId: string,
    @Param('id', ParseUUIDPipe) id: string,
  ) {
    return this.service.findOne(id, tenantId);
  }

  @Post()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({ summary: 'Crea __MODULE_SINGULAR_IT__' })
  async create(
    @TenantId() tenantId: string,
    @Body() dto: Create__MODULE_NAME__Dto,
  ) {
    return this.service.create(tenantId, dto);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Aggiorna __MODULE_SINGULAR_IT__' })
  async update(
    @TenantId() tenantId: string,
    @Param('id', ParseUUIDPipe) id: string,
    @Body() dto: Update__MODULE_NAME__Dto,
  ) {
    return this.service.update(id, tenantId, dto);
  }

  @Delete(':id')
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({ summary: 'Elimina __MODULE_SINGULAR_IT__' })
  async remove(
    @TenantId() tenantId: string,
    @Param('id', ParseUUIDPipe) id: string,
  ) {
    return this.service.remove(id, tenantId);
  }
}
