{
  "mcpServers": {
    "postgres": {
      "type": "stdio",
      "command": "/bin/sh",
      "args": [
        "-c",
        "set -a && . '/Users/romanogiovanni1993gmail.com/Desktop/Nexo gestionale/backend/.env' && set +a && npx -y @modelcontextprotocol/server-postgres \"$DATABASE_URL\""
      ]
    }
  }
}
